<?xml version="1.0" ?>
<odoo>
    <data>
<!-- Inherited View of Salary Rule form -->
        <record model="ir.ui.view" id="hr_salary_rule_view_extension">
            <field name="name">hr.salary.rule.form.extension</field>
            <field name="model">hr.salary.rule</field>
            <field name="inherit_id" ref="hr_payroll.hr_salary_rule_form" />
            <field name="arch" type="xml">
                <xpath expr="/form/group/field[@name='active']" position="after">
                    <field name="is_cpf"/>
                </xpath>
            </field>
        </record>

<!-- Data For Salary Rules -->
        <record id="l10n_sg_hr_payroll.hr_salary_rule_cpfemployeesprsingaporean0" model="hr.salary.rule">
            <field name="code">CPFEE_SPR_SIN</field>
            <field eval="601" name="sequence"/>
            <field eval="1" name="appears_on_payslip"/>
            <field name="condition_range">contract.wage</field>
            <field name="company_id" ref="base.main_company"/>
            <field name="condition_select">python</field>
            <field name="register_id" ref="l10n_sg_hr_payroll.hr_contribution_register_10"/>
            <field name="amount_select">code</field>
            <field eval="1" name="active"/>
            <field name="name">CPF Employee</field>
            <field name="condition_python">if categories.GROSS &gt; 49:
  result = True
else:
  result = False</field>
            <field name="amount_python_compute">
if not employee.singaporean and employee.pr_year == 1:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.15)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05))
            else:
                result =  math.floor(categories.GROSS * 0.05)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.15)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05))
            else:
                result =  math.floor(categories.GROSS * 0.05)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.15)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05))
            else:
                result =  math.floor(categories.GROSS * 0.05)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.15)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05))
            else:
                result =  math.floor(categories.GROSS * 0.05)
        else:
            result=0
elif not employee.singaporean and employee.pr_year == 2:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.15)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05))
            else:
                result =  math.floor(categories.GROSS * 0.05)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.225)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.075) + (aw_total * 0.075))
            else:
                result =  math.floor(categories.GROSS * 0.075)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.375)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.125) + (aw_total * 0.125))
            else:
                result =  math.floor(categories.GROSS * 0.125)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.45)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.15) + (aw_total * 0.15))
            else:
                result = math.floor(categories.GROSS * 0.15)
        else:
            result=0
elif employee.singaporean or employee.pr_year &gt;= 3:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.15)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05))
            else:
                result =  math.floor(categories.GROSS * 0.05)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.225)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.075) + (aw_total * 0.075))
            else:
                result =  math.floor(categories.GROSS * 0.075)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.39)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.13) + (aw_total * 0.13))
            else:
                result =  math.floor(categories.GROSS * 0.13)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result =  math.floor((categories.GROSS - 500) * 0.6)
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.20) + (aw_total * 0.20))
            else:
                result =  math.floor(categories.GROSS * 0.20)
        else:
            result=0
else:
    result=0</field>
            <field name="category_id" ref="l10n_sg_hr_payroll.hr_salary_rule_category_18"/>
            <field name="quantity">1.0</field>
        </record>

        <record id="l10n_sg_hr_payroll.hr_salary_rule_cpftotalsprsingaporean0" model="hr.salary.rule">
            <field name="code">CPFTOTAL_SPR_SIN</field>
            <field eval="602" name="sequence"/>
            <field eval="1" name="appears_on_payslip"/>
            <field name="condition_range">contract.wage</field>
            <field name="company_id" ref="base.main_company"/>
            <field name="condition_select">python</field>
            <field name="amount_select">code</field>
            <field eval="1" name="active"/>
            <field name="name">CPF Total</field>
            <field name="condition_python">if categories.GROSS &gt; 49:
  result = True
else:
  result = False</field>
            <field name="amount_python_compute">
if not employee.singaporean and employee.employee_type=='graduated_employee' and employee.pr_year == 1:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.085) + (aw_total * 0.085))
            else:
                result =  round(categories.GROSS * 0.085)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.085) + (aw_total * 0.085))
            else:
                result =  round(categories.GROSS * 0.085)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.04 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.04 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.09) + (aw_total * 0.09))
            else:
                result =  round(categories.GROSS * 0.09)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.04 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.04 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.09) + (aw_total * 0.09))
            else:
                result =  round(categories.GROSS * 0.09)
        else:
            result=0
elif not employee.singaporean and employee.employee_type=='full_employeement' and employee.pr_year == 1:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result =  round(0.075 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.075 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.125) + (aw_total * 0.125))
            else:
                result =  round(categories.GROSS * 0.125)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.14) + (aw_total * 0.14))
            else:
                result =  round(categories.GROSS * 0.14)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.13 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.13 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.18) + (aw_total * 0.18))
            else:
                result =  round(categories.GROSS * 0.18)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.17 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.17 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.22) + (aw_total * 0.22))
            else:
                result =  round(categories.GROSS * 0.22)
        else:
            result=0
elif not employee.singaporean and employee.employee_type=='graduated_employee' and employee.pr_year == 2:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.085) + (aw_total * 0.085))
            else:
                result =  round(categories.GROSS * 0.085)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.225))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.11) + (aw_total * 0.11))
            else:
                result =  round(categories.GROSS * 0.11)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.06 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.06 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.375))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.185) + (aw_total * 0.185))
            else:
                result =  round(categories.GROSS * 0.185)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.45))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.24) + (aw_total * 0.24))
            else:
                result =  round(categories.GROSS * 0.24)
        else:
            result=0
elif not employee.singaporean and employee.employee_type=='full_employeement' and employee.pr_year == 2:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result =  round(0.075 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.075 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.125) + (aw_total * 0.125))
            else:
                result =  round(categories.GROSS * 0.125)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.225))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.165) + (aw_total * 0.165))
            else:
                result =  round(categories.GROSS * 0.165)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.13 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.13 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.375))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.255) + (aw_total * 0.255))
            else:
                result =  round(categories.GROSS * 0.255)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.17 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.17 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.45))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.32) + (aw_total * 0.32))
            else:
                result =  round(categories.GROSS * 0.32)
        else:
            result=0
elif employee.singaporean or employee.pr_year &gt;= 3:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result =  round(0.075 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.075 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.125) + (aw_total * 0.125))
            else:
                result =  round(categories.GROSS * 0.125)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.225))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.165) + (aw_total * 0.165))
            else:
                result =  round(categories.GROSS * 0.165)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.13 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.13 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.39))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.26) + (aw_total * 0.26))
            else:
                result =  round(categories.GROSS * 0.26)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.17 * (categories.GROSS))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.17 * (categories.GROSS) + math.floor((categories.GROSS - 500) * 0.6))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.37) + (aw_total * 0.37))
            else:
                result =  round(categories.GROSS * 0.37)
        else:
            result=0
else:
    result = 0</field>
            <field name="category_id" ref="l10n_sg_hr_payroll.hr_salary_rule_category_17"/>
            <field name="quantity">1.0</field>
        </record>

        <record id="hr_salary_rule_cpfemployees_ow" model="hr.salary.rule">
            <field name="code">CPFEE_SPR_SIN_OW</field>
            <field eval="611" name="sequence"/>
            <field eval="1" name="appears_on_payslip"/>
            <field name="condition_range">contract.wage</field>
            <field name="company_id" ref="base.main_company"/>
            <field name="condition_select">python</field>
            <field name="amount_select">code</field>
            <field eval="1" name="active"/>
            <field name="name">Ordinary Wages (CPF Total)</field>
            <field name="condition_python">if categories.GROSS &gt; 49:
  result = True
else:
  result = False</field>
            <field name="amount_python_compute">
if not employee.singaporean and employee.employee_type=='graduated_employee' and employee.pr_year == 1:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.085) + (aw_total * 0.085 / 2))
            else:
                result =  round(ow_total * 0.085)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.085) + (aw_total * 0.085 / 2))
            else:
                result =  round(ow_total * 0.085)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.04 * (ow_total))
        elif catgories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.04 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.09) + (aw_total * 0.09 / 2))
            else:
                result =  round(ow_total * 0.09)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.04 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.04 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.09) + (aw_total * 0.09 / 2))
            else:
                result =  round(ow_total * 0.09)
        else:
            result=0
elif not employee.singaporean and employee.employee_type=='full_employeement' and employee.pr_year == 1:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result =  round(0.075 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.075 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.125) + (aw_total * 0.125 / 2))
            else:
                result =  round(ow_total * 0.125)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.14) + (aw_total * 0.14 / 2))
            else:
                result =  round(ow_total * 0.14)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.13 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.13 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.18) + (aw_total * 0.18 / 2))
            else:
                result =  round(ow_total * 0.18)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.17 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.17 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.22) + (aw_total * 0.22 / 2))
            else:
                result =  round(ow_total * 0.22)
        else:
            result=0
elif not employee.singaporean and employee.employee_type=='graduated_employee' and employee.pr_year == 2:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.085) + (aw_total * 0.085 / 2))
            else:
                result =  round(ow_total * 0.085)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (ow_total) + math.floor((ow_total - 250) * 0.225))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.11) + (aw_total * 0.11 / 2))
            else:
                result =  round(ow_total * 0.11)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.06 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.06 * (ow_total) + math.floor((ow_total - 250) * 0.375))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.185) + (aw_total * 0.185 / 2))
            else:
                result =  round(ow_total * 0.185)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (ow_total) + math.floor((ow_total - 250) * 0.45))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.24) + (aw_total * 0.24 / 2))
            else:
                result =  round(ow_total * 0.24)
        else:
            result=0
elif not employee.singaporean and employee.employee_type=='full_employeement' and employee.pr_year == 2:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result =  round(0.075 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.075 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.125) + (aw_total * 0.125 / 2))
            else:
                result =  round(ow_total * 0.125)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (ow_total) + math.floor((ow_total - 250) * 0.225))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.165) + (aw_total * 0.165 / 2))
            else:
                result =  round(ow_total * 0.165)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.13 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.13 * (ow_total) + math.floor((ow_total - 250) * 0.375))
        elif categories.GROSS &gt;= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.255) + (aw_total * 0.255 / 2))
            else:
                result =  round(ow_total * 0.255)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.17 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.17 * (ow_total) + math.floor((ow_total - 250) * 0.45))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.32) + (aw_total * 0.32 / 2))
            else:
                result =  round(ow_total * 0.32)
        else:
            result=0
elif employee.singaporean or employee.pr_year &gt;= 3:
    if employee.age &gt;= 66:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result =  round(0.075 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.075 * (ow_total) + math.floor((ow_total - 250) * 0.15))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.125) + (aw_total * 0.125 / 2))
            else:
                result =  round(ow_total * 0.125)
        else:
            result=0
    elif employee.age &gt;= 61 and employee.age &lt;=65:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (ow_total) + math.floor((ow_total - 250) * 0.225))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.165) + (aw_total * 0.165 / 2))
            else:
                result =  round(ow_total * 0.165)
        else:
            result=0
    elif employee.age &gt;= 56 and employee.age &lt;=60:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.13 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.13 * (ow_total) + math.floor((ow_total - 250) * 0.39))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.26) + (aw_total * 0.26 / 2))
            else:
                result =  round(ow_total * 0.26)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS &gt; 50 and categories.GROSS &lt;= 500:
            result = round(0.17 * (ow_total))
        elif categories.GROSS &gt; 500 and categories.GROSS &lt; 750:
            result = round(0.17 * (ow_total) + math.floor((ow_total - 250) * 0.6))
        elif categories.GROSS &gt;= 750:
            if ow_total &gt; 6000:
                result =  math.floor((6000 * 0.37) + (aw_total * 0.37 / 2))
            else:
                result =  round(ow_total * 0.37)
        else:
            result=0
else:
    result = 0
            </field>
            <field name="category_id" ref="hr_payroll.OW"/>
            <field name="quantity">1.0</field>
        </record>

        <record id="hr_salary_rule_cpfemployees_aw" model="hr.salary.rule">
            <field name="code">CPFEE_SPR_SIN_AW</field>
            <field eval="612" name="sequence"/>
            <field eval="1" name="appears_on_payslip"/>
            <field name="condition_range">contract.wage</field>
            <field name="company_id" ref="base.main_company"/>
            <field name="condition_select">python</field>
            <field name="amount_select">code</field>
            <field eval="1" name="active"/>
            <field name="name">Additional Wages (CPF Total)</field>
            <field name="condition_python">if categories.GROSS &gt; 49:
  result = True
else:
  result = False</field>
            <field name="amount_python_compute">
if not employee.singaporean and employee.employee_type=='graduated_employee' and employee.pr_year == 1:
    if employee.age >= 66:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.085)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.085)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.04 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.04 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.09)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.04 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.04 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.09)
        else:
            result=0
elif not employee.singaporean and employee.employee_type=='full_employeement' and employee.pr_year == 1:
    if employee.age >= 66:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result =  round(0.075 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.075 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
             result =  round(aw_total * 0.125)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.14)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.13 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.13 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.18)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.17 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.17 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.22)
        else:
            result=0
elif not employee.singaporean and employee.employee_type=='graduated_employee' and employee.pr_year == 2:
    if employee.age >= 66:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.085)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.035 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.035 * (aw_total) + math.floor((aw_total - 250) * 0.225))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.11)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.06 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.06 * (aw_total) + math.floor((aw_total - 250) * 0.375))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.185)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (aw_total) + math.floor((aw_total - 250) * 0.45))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.24)
        else:
            result=0
elif not employee.singaporean and employee.employee_type=='full_employeement' and employee.pr_year == 2:
    if employee.age >= 66:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result =  round(0.075 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.075 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.125)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (aw_total) + math.floor((aw_total - 250) * 0.225))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.165)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.13 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.13 * (aw_total) + math.floor((aw_total - 250) * 0.375))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.255)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.17 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.17 * (aw_total) + math.floor((aw_total - 250) * 0.45))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.32)
        else:
            result=0
elif employee.singaporean or employee.pr_year >= 3:
    if employee.age >= 66:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result =  round(0.075 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.075 * (aw_total) + math.floor((aw_total - 250) * 0.15))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.125)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.09 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.09 * (aw_total) + math.floor((aw_total - 250) * 0.225))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.165)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.13 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.13 * (aw_total) + math.floor((aw_total - 250) * 0.39))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.26)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 50 and categories.GROSS &lt;= 500:
            result = round(0.17 * (aw_total))
        elif categories.GROSS > 500 and categories.GROSS &lt; 750:
            result = round(0.17 * (aw_total) + math.floor((aw_total - 250) * 0.6))
        elif categories.GROSS >= 750:
                result =  round(aw_total * 0.37)
        else:
            result=0
else:
    result = 0
            </field>
            <field name="category_id" ref="hr_payroll.AW"/>
            <field name="quantity">1.0</field>
        </record>

        <record id="hr_salary_rule_ow_cpf_employee" model="hr.salary.rule">
            <field name="code">CPFEE_SPR_SIN_OW_EMP</field>
            <field eval="613" name="sequence"/>
            <field eval="1" name="appears_on_payslip"/>
            <field name="condition_range">contract.wage</field>
            <field name="company_id" ref="base.main_company"/>
            <field name="condition_select">python</field>
            <field name="amount_select">code</field>
            <field eval="1" name="active"/>
            <field name="name">Ordinary Wages (CPF Employee)</field>
            <field name="condition_python">if categories.GROSS &gt; 49:
  result = True
else:
  result = False</field>
            <field name="amount_python_compute">
if not employee.singaporean and employee.pr_year == 1:
    if employee.age >= 66:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.15)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05 / 2))
            else:
                result =  math.floor(ow_total * 0.05)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.15)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05 / 2))
            else:
                result =  math.floor(ow_total * 0.05)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.15)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05 / 2))
            else:
                result =  math.floor(ow_total * 0.05)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.15)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05 / 2))
            else:
                result =  math.floor(ow_total * 0.05)
        else:
            result=0
elif not employee.singaporean and employee.pr_year == 2:
    if employee.age >= 66:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.15)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05 / 2))
            else:
                result =  math.floor(ow_total * 0.05)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.225)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.075) + (aw_total * 0.075 / 2))
            else:
                result =  math.floor(ow_total * 0.075)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.375)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.125) + (aw_total * 0.125 / 2))
            else:
                result =  math.floor(ow_total * 0.125)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.45)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.15) + (aw_total * 0.15 / 2))
            else:
                result = math.floor(ow_total * 0.15)
        else:
            result=0
elif employee.singaporean or employee.pr_year >= 3:
    if employee.age >= 66:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.15)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.05) + (aw_total * 0.05 / 2))
            else:
                result =  math.floor(ow_total * 0.05)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.225)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.075) + (aw_total * 0.075 / 2))
            else:
                result =  math.floor(ow_total * 0.075)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.39)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.13) + (aw_total * 0.13 / 2))
            else:
                result =  math.floor(ow_total * 0.13)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((ow_total - 250) * 0.6)
        elif categories.GROSS >= 750:
            if ow_total > 6000:
                result =  math.floor((6000 * 0.20) + (aw_total * 0.20 / 2))
            else:
                result =  math.floor(ow_total * 0.20)
        else:
            result=0
else:
    result=0</field>
            <field name="category_id" ref="hr_payroll.OW"/>
            <field name="quantity">1.0</field>
        </record>

        <record id="hr_salary_rule_aw_cpf_employee" model="hr.salary.rule">
            <field name="code">CPFEE_SPR_SIN_AW_EMP</field>
            <field eval="614" name="sequence"/>
            <field eval="1" name="appears_on_payslip"/>
            <field name="condition_range">contract.wage</field>
            <field name="company_id" ref="base.main_company"/>
            <field name="condition_select">python</field>
            <field name="amount_select">code</field>
            <field eval="1" name="active"/>
            <field name="name">Additional Wages (CPF Employee)</field>
            <field name="condition_python">if categories.GROSS &gt; 49:
  result = True
else:
  result = False</field>
            <field name="amount_python_compute">
if not employee.singaporean and employee.pr_year == 1:
    if employee.age >= 66:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.15)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.05)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.15)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.05)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.15)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.05)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.15)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.05)
        else:
            result=0
elif not employee.singaporean and employee.pr_year == 2:
    if employee.age >= 66:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.15)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.05)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.225)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.075)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.375)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.125)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.45)
        elif categories.GROSS >= 750:
                result = math.floor(aw_total * 0.15)
        else:
            result=0
elif employee.singaporean or employee.pr_year >= 3:
    if employee.age >= 66:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.15)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.05)
        else:
            result=0
    elif employee.age >= 61 and employee.age &lt;=65:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.225)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.075)
        else:
            result=0
    elif employee.age >= 56 and employee.age &lt;=60:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.39)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.13)
        else:
            result=0
    elif employee.age &lt;=55:
        if categories.GROSS > 500 and categories.GROSS &lt; 750:
            result =  math.floor((aw_total - 250) * 0.6)
        elif categories.GROSS >= 750:
                result =  math.floor(aw_total * 0.20)
        else:
            result=0
else:
    result=0</field>
            <field name="category_id" ref="hr_payroll.AW"/>
            <field name="quantity">1.0</field>
        </record>
     </data>
</odoo>