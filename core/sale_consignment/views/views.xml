<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sale_consignment.list">
      <field name="name">sale_consignment list</field>
      <field name="model">sale_consignment.sale_consignment</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sale_consignment.action_window">
      <field name="name">sale_consignment window</field>
      <field name="res_model">sale_consignment.sale_consignment</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sale_consignment.action_server">
      <field name="name">sale_consignment server</field>
      <field name="model_id" ref="model_sale_consignment_sale_consignment"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sale_consignment" id="sale_consignment.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sale_consignment.menu_1" parent="sale_consignment.menu_root"/>
    <menuitem name="Menu 2" id="sale_consignment.menu_2" parent="sale_consignment.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sale_consignment.menu_1_list" parent="sale_consignment.menu_1"
              action="sale_consignment.action_window"/>
    <menuitem name="Server to list" id="sale_consignment" parent="sale_consignment.menu_2"
              action="sale_consignment.action_server"/>
    -->
  </data>
</odoo>