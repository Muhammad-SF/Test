<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="scrap_accounting.list">
      <field name="name">scrap_accounting list</field>
      <field name="model">scrap_accounting.scrap_accounting</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="scrap_accounting.action_window">
      <field name="name">scrap_accounting window</field>
      <field name="res_model">scrap_accounting.scrap_accounting</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="scrap_accounting.action_server">
      <field name="name">scrap_accounting server</field>
      <field name="model_id" ref="model_scrap_accounting_scrap_accounting"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="scrap_accounting" id="scrap_accounting.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="scrap_accounting.menu_1" parent="scrap_accounting.menu_root"/>
    <menuitem name="Menu 2" id="scrap_accounting.menu_2" parent="scrap_accounting.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="scrap_accounting.menu_1_list" parent="scrap_accounting.menu_1"
              action="scrap_accounting.action_window"/>
    <menuitem name="Server to list" id="scrap_accounting" parent="scrap_accounting.menu_2"
              action="scrap_accounting.action_server"/>
    -->
  </data>
</odoo>